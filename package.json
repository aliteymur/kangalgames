{
  "name": "kangalgames",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Official website for Kangal Games — an independent game studio building bold, high-craft games.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "android:add": "cap add android",
    "android:open": "npm run build && cap sync android && cap open android",
    "ios:add": "cap add ios",
    "ios:open": "npm run build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^8.1.3"
  }
}
